name: pexels_clean_arch
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  equatable: ^2.0.5
  dartz: ^0.10.1
  mocktail: ^1.0.4
  http: ^1.2.2
  get_it: ^7.7.0
  flutter_triple: ^3.0.0
  go_router: ^14.2.1
  cached_network_image: ^3.3.1
  phosphor_flutter: ^2.1.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
